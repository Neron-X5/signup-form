import React from 'react';

import SignupContainer from '../signup-container';

import './style.css';

const AppComponent = () => {
    return (
        <div className="app">
            <SignupContainer />
        </div>
    );
};

export default AppComponent;
